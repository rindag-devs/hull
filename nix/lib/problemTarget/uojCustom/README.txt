Set the UOJ problem extra_config to the following JSON before syncing data:

{"dont_use_formatter": true}

This disables UOJ's formatter during sync. Without it, UOJ may corrupt
binary files such as a custom judger executable.
