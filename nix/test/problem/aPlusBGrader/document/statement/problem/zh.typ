#let description = [
  *这是一道 grader 交互题．*

  给定两个整数 $A$ 和 $B$．计算这两个整数的和．

  你必须在你的源代码中 include `add.h` 头文件．

  你应该实现下面的函数：

  ```c
  int32_t add(int32_t a, int32_t b);
  ```

  该函数接收两个 32-bit 整数，`a` 和 `b`，返回它们的和．
]

#let input = none

#let output = none

#let notes = [
  你提交的源代码禁止包括 `main` 函数．

  $-1000 <= A, B <= 1000$．
]
