{ ... }:

{
  displayName.en = "MST";
  traits = {
    w_eq_1.descriptions.en = "For each potential bridge, $w=1$.";
  };
  testCases.smallUnweighted.descriptions.en = "The edge weights are all 1, so any spanning tree can be chosen.";
}
